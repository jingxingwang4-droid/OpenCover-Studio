# OpenCover Studio 项目需求基线

> 状态：长期有效的产品与验收基线  
> 来源：用户最初的 53 节完整需求，以及后续在同一任务中的补充要求  
> 用途：后续新对话、开发者和自动化代理必须先读本文，再根据当前工作区继续实现、验证和修复

本文描述的是最终目标，不是对当前实现状态的宣传。已有实现、测试报告或发行包不能自动证明本文全部完成；每次声称交付完成前，都必须逐条检查本文的验收清单并给出真实证据。

## 1. 项目目标与不可变原则

项目名称为 **OpenCover Studio**。它是面向普通用户的本地 Windows AI 歌曲翻唱桌面软件：用户拖入歌曲，选择 RVC 或 DDSP 及音色，点击生成，软件自动完成音源分离、音色转换、校正、响度匹配、混音、试听和导出。

同时提供实验性的“改词翻唱 Beta”，尽量保留原曲旋律和节奏，用用户提供的新歌词替换原歌词。

优先级：

1. 小白可正常使用；
2. 原词翻唱稳定可靠；
3. 操作步骤少、流程自动；
4. 界面像成熟 Windows 音频工具，而不是 AI Demo；
5. 支持用户导入 RVC/DDSP 音色；
6. 音色具有头像、名称、简介和真实试听；
7. 支持低显存及较老 NVIDIA 显卡；
8. 错误清楚，任务可取消、恢复并复用缓存；
9. 在上述目标之后再追求极致音质和专业参数。

严禁：

- 用 mock、复制原音频、静音 WAV、空白 WAV、随机音频或其他模型输出冒充真实能力；
- 捏造资源 URL、作者、模型身份、版本、许可证、再分发权或测试状态；
- 在 GUI 主线程加载重型模型；
- 使用 `shell=True` 拼接用户输入；
- 把多个重型后端塞进同一个 Python 环境；
- 宣称未经实体硬件或真实推理验证的兼容性；
- 为了让测试容易通过而把需求替换成较窄的空壳实现。

不在范围内：TTS、模型训练、实时变声、DAW、钢琴卷帘、多轨手工编辑、OpenUtau 集成、云推理、账号、在线社区和模型市场。

原创代码使用 MIT；PySide6/Qt、第三方程序、模型、权重、素材分别遵守其许可证。

## 2. 用户后续补充要求

以下要求与最初需求具有同等效力：

- 用户已在 `assets/` 提供三张背景图，优先使用第一张；不轮播。祥子音色头像也由用户提供。
- 除用户提供的祥子图片外，所需后端、模型、工具和其他资源应主动从可信网络来源获取，不依赖电脑其他目录里的旧整合包。
- 双击 `app.py` 必须能打开窗口；失败时必须诊断并修复，而不是只让用户从终端启动。
- 用户本地代理为 `127.0.0.1:7897`。网络操作可设置 `HTTP_PROXY`/`HTTPS_PROXY=http://127.0.0.1:7897`，网速慢时应延长超时并支持续传。
- `assets/audio/春日影.wav` 是后续真实翻唱测试曲。它是用户文件，不得进入公开发行包或 Git。
- 丰川祥子 RVC/DDSP 权重需要联网寻找、核验和真实测试；授权不清的社区权重只能本机使用，不得冒充官方或擅自公开再分发。
- 继续自主推进能够安全完成的工作；单个后端受阻时记录原因并做其他项目，不反复请求确认。

## 3. 技术栈与桌面形态

必须使用：

- Python 3.10；
- PySide6 + Qt Widgets（不是 QML）；
- SQLite、Pydantic、PyYAML、`pathlib`；
- FFmpeg、soundfile、pyloudnorm、pytest；
- GUI → JobManager → QProcess/subprocess → 独立 Worker 的进程架构；
- UTF-8 JSON Lines 作为工作进程状态/进度/结果/错误协议。

不得使用 Gradio、Streamlit、浏览器 WebUI、React、Vue、Electron、Tauri、Docker、Redis、Celery、PostgreSQL 或云 API。

目标启动体验：双击 `OpenCoverStudio.exe` 或源码阶段双击 `app.py`，直接出现桌面窗口；不打开浏览器、不显示用户可见控制台、不要求本地服务地址或保持终端开启。

Windows GUI 必须支持最小化、最大化、系统托盘、窗口尺寸和最后页面记忆、中文路径，以及 125%/150%/200% DPI 缩放。

## 4. 视觉与页面结构

界面全部使用简体中文。采用白/浅灰/米白主体、深灰正文、单一克制强调色、适中圆角、极少阴影和合理信息密度。禁止满屏蓝紫渐变、玻璃拟态、霓虹、发光、粒子、视频背景、漂浮卡片、巨型营销标题、过度胶囊化与空洞 AI 文案。

主窗口为左侧窄导航、右侧内容区，包含七页：

1. 首页；
2. 原词翻唱；
3. 改词翻唱 Beta；
4. 音色管理；
5. 任务记录；
6. 组件管理；
7. 设置。

侧栏底部应显示 GPU、显存模式、后端状态和软件版本。

首页只突出“原词翻唱”和“改词翻唱 Beta”，并显示最近生成、推荐音色、硬件状态、必要组件状态、导入音色入口。基础组件缺失时明确显示“一键安装/查看组件”，不伪装为可用。

## 5. 背景、头像与素材规则

背景支持 PNG/JPG/JPEG/WebP。一张时全局使用；多张时优先第一张，可按页面分配但不轮播、不动态切换、不做视差、不平铺。无图时使用纯色或克制渐变且不报错。必须加浅色蒙层保证文字可读。不得联网寻找祥子图片。

所有内置音色必须有头像。祥子只用用户提供图片；其他音色优先使用作者/官方/明确授权素材，无法确认授权时使用项目生成的 512×512 居中首字母占位头像。上传头像支持 PNG/JPG/JPEG/WebP，限制大小、检查真实图片格式、修正 EXIF、居中裁切、不拉伸，保存原图与 WebP 缓存。

推荐资源目录：

```text
assets/
├─ backgrounds/
├─ icons/
├─ preview_sources/
└─ voice_avatars/
   ├─ bundled/
   ├─ user/
   └─ placeholders/
```

## 6. 资源调查、清单与许可证

主动联网调查并获取 FFmpeg、MSST 与分离模型、RVC、DDSP-SVC、普通及祥子双模型、初始音色、Vevo2、GAME、DiffSinger、vocoder、G2P、歌词对齐、测试音频、合法标准试听干声和必要 PyTorch/CUDA Runtime。

来源优先级：官方 GitHub、官方 Release、官方 Hugging Face/ModelScope、作者下载地址、来源及许可证清楚的社区资源。避免不明网盘、转载、不透明下载器、要求关闭杀毒软件的包、格式不明模型或带可疑脚本的模型包。

`config/resource_manifest.yaml` 中每项资源必须记录：

- `resource_id`、类型、名称、作者；
- 官方仓库、来源页面、直接下载地址；
- 版本、tag、固定 commit/revision；
- 精确文件大小与 SHA256；
- 许可证和是否允许再分发；
- 安装目录、所属后端、最后验证日期；
- 当前真实状态以及支持该状态的证据。

同时维护 `RESOURCE_SOURCES.md`、`THIRD_PARTY_NOTICES.md`、`docs/RESOURCE_RESEARCH.md` 和随包许可证/署名。授权不清的模型允许本机研究和测试时，必须明确标成 local-only，并从 Git 与公开发行排除。

## 7. 下载器与组件安装

提供 `scripts/download_resources.py` 和组件管理 GUI。下载系统应支持 HTTP/HTTPS、GitHub、Hugging Face、ModelScope、断点续传、进度、速度、剩余大小、有限重试、SHA256、磁盘检查、缓存、取消、下载后安装。

安全要求：

- 检测 HTML 页面冒充二进制；
- 校验大小与 SHA256 后才安装；
- ZIP 防路径穿越、符号链接、压缩炸弹和异常大小；
- 模型包默认拒绝脚本与可执行文件；
- 不自动执行模型包脚本；
- 不覆盖用户文件；
- 安装目录必须限制在项目允许根目录内。

组件管理显示 FFmpeg、MSST、RVC、DDSP、Vevo2、GAME、DiffSinger、对齐模型，区分已安装、未安装、需要更新、损坏、不兼容、模型缺失。提供安装、修复、检测、打开目录、版本、许可证和可选组件卸载。

“已安装/可用”必须以文件检查和真实 smoke test 为依据，不能只看目录存在。

## 8. 原词翻唱核心流程

用户流程：拖入/选择歌曲 → 选择 RVC 或 DDSP → 选择该引擎音色 → 开始翻唱 → 软件内试听 → 保存或打开输出目录。

内部流程：

```text
输入音频 → 格式标准化 → MSST 分离 → 自动音域/升降调
→ RVC 或 DDSP 转换 → 延迟校正 → 时长对齐
→ 响度匹配 → 自动混音 → 导出
```

默认参数：自动升降调、推荐分离质量、均衡混音、WAV。高级项：-12～+12、快速/推荐、人声更突出/均衡/伴奏更突出、WAV/FLAC/MP3、保留中间文件。

MSST 必须固定维护版本/commit、独立环境和模型，并完成真实分离测试，记录耗时与显存。普通用户只看到“快速/推荐”，不看到 MSST 模型名。

RVC 必须有 Windows 稳定 CLI/worker、可靠 F0（如 RMVPE）、独立环境、错误/进度捕获和显存释放。至少真实测试普通 RVC 与丰川祥子 RVC。

DDSP-SVC 必须根据实际版本/格式适配，不可把不同格式写死。至少真实测试普通 DDSP 与丰川祥子 DDSP，并能产生真正 WAV、捕获 OOM。适配器公开动态 `ModelImportSchema`（必需文件、可选文件、可接受扩展名）。

## 9. 引擎、初始音色和固定 ID

所有选择界面先选 RVC/DDSP，再只显示对应音色。切换引擎时必须清除旧选择并刷新模型、头像、简介、试听、高级设置和后端状态，禁止混合列表或保留错误模型 ID。

目标基础包初始音色：RVC 3～5 个、DDSP 3～5 个。每个音色必须身份/来源清楚、许可可记录、可真实推理、歌唱效果合理、体积合理、有合法头像或占位头像、有该模型的真实试听。

必须寻找并测试：

```text
toyokawa_sakiko_rvc
toyokawa_sakiko_ddsp
```

如果祥子社区模型未获明确再分发许可，应保留固定 ID、本机安装与真实测试证据，但不得进入公开包。不可把非官方社区模型描述为官方。

## 10. 音色试听系统（强制）

试听优先级：用户上传的真实试听 → 模型包自带且校验过的试听 → 内置合法标准干声经目标模型真实生成。

严禁直接播放标准干声冒充转换结果、复用其他模型试听、复制输入、静音或随机音频。

至少提供 `assets/preview_sources/neutral_melody.wav`：无伴奏、少混响、8～15 秒、覆盖低中高音域、不过分极端，且来源和再分发许可明确。可选增加 `female_mid.wav`、`male_mid.wav`。

用户导入无试听模型时：校验模型 → 检查后端 → 后台独立任务用标准干声推理 → 验证 WAV 非静音且不是输入复制 → 原子写入模型目录 `preview.wav` → 更新来源与 SHA256。任务有进度、可取消、失败可见且不阻塞 GUI。

后端缺失时仍允许导入，但卡片显示“安装后生成试听/生成试听”，不得显示“试听”。用户修改 pitch、RVC index、DDSP 配置或试听源后可重新生成；默认用推荐 pitch，没有则为 0。相同模型哈希、Key 和源音频可命中缓存，不能跨模型复用。

上传试听支持 WAV/FLAC/MP3/M4A（FFmpeg 可解析时），限制大小和最长时长，转换到统一 `preview.wav`，保留 `preview_original.*` 和来源记录，不覆盖原文件。

## 11. 音色导入、卡片和管理

首页、原词页、改词页、音色管理页均提供导入入口。流程：选择引擎、权重、可选 index/配置、名称、简介、可选头像、试听方式（自动/上传/暂不），导入后无需重启立即刷新。

RVC 支持 `.pth/.pt` 和可选 `.index`；无 index 时提示但仍可用。DDSP 根据实际 adapter 要求选择权重+配置或完整目录。所有导入需校验存在性、扩展名、合理大小、基础格式、SHA256、重复权重和唯一安全 ID；支持中文路径且不覆盖现有模型。

模型目录约定：

```text
weights/<rvc|ddsp>/<bundled|user_models>/<model_id>/
├─ model.pth / model.pt / model.ckpt
├─ model.index / config.yaml（按引擎可选或必需）
├─ avatar_original.*
├─ avatar.webp
├─ preview_original.*
├─ preview.wav
└─ model.json
```

`model.json` 至少保存 ID、显示名、简介、engine、角色/系列（如适用）、模型/index/config 文件、头像、试听及来源、推荐 pitch、语言、作者、来源、许可证、再分发标记、各文件 SHA256、内置/精选/排序信息。

音色卡显示头像、名称、简介、内置/用户、语言、试听状态和选择按钮。大量音色支持搜索、置顶、滚动、名称与最近使用排序。

音色管理支持编辑名称/简介/pitch/语言、换或删头像、换/删/重生试听、置顶、隐藏内置、删除用户模型、打开模型目录；元数据修改不得改变权重，内置模型不得被普通删除操作移除。

如支持 ZIP 导入，必须防路径穿越/压缩炸弹，限制大小，只允许模型、配置、图片和音频，拒绝脚本/可执行文件。

## 12. 改词翻唱 Beta

页面明确提示“复杂歌词可能出现咬字、节奏或旋律偏差”。用户提供歌曲、原歌词、新歌词、引擎和音色。

主流程：

```text
MSST → 歌词对齐 → 自动切句 → 新歌词分配 → Vevo2
→ 时长校正 → 拼接 → RVC/DDSP → 混音
```

回退流程仅在条件满足时启用：

```text
Vevo2 失败 → GAME → G2P → 音符映射 → DiffSinger
→ RVC/DDSP → 混音
```

失败时不导出 OpenUtau、不要求用户人工处理 MIDI。

歌词支持粘贴、TXT、LRC、UTF-8、GBK 和常见日文编码；处理时间戳、标签、空行、标点、重复副歌及中/日/英。短句优先 3～15 秒。新歌词策略为保守/均衡/强制，默认自动均衡；较短可延长元音，略长可提高局部密度，明显过长应重分句，仍失败则提示修改，不能无限加速。

Vevo2 必须固定 Amphion commit、模型、tokenizer、vocoder和许可证，放在独立环境；中文与日文都需真实推理后才可标为可用，并记录显存与速度。

GAME + DiffSinger 必须固定源码、权重、歌声模型、vocoder、G2P、音素表；先以固定句真实生成 WAV，再接入主流程。语言未实现时必须清楚限制，不能静默产生伪结果。

歌词对齐工具及模型同样需要固定版本、来源、哈希、独立环境和真实歌声测试。

## 13. 任务、缓存、历史与托盘

所有推理离开 GUI 主线程。Worker 用 JSON Lines 发送 `status`、`progress`、`result`、`error`，后端崩溃不得退出 GUI。

状态：`pending/running/completed/failed/cancelled`。

原词阶段：`validate → normalize → separate → pitch → convert → align → mix → export`。  
改词阶段：`validate → normalize → separate → align_lyrics → segment → redistribute → generate → fit_duration → stitch → convert → mix → export`。

缓存必须按输入、后端/模型哈希和相关参数分层：换音色不重分离；改词后换音色不重新改词；调混音/格式不重新模型推理；试听独立缓存；模型、Key、试听源不变时不重生试听，任一变化时必须失效。

任务记录显示歌曲、引擎、头像、音色、类型、状态、时间和输出，并支持播放、打开目录、重新生成、换同引擎音色、删除记录、清缓存、导出日志。

运行任务时关闭窗口提供“继续后台运行 / 取消任务并退出 / 返回软件”。托盘支持打开软件、进度、打开输出、取消和退出；任务完成发送 Windows 通知。取消重型任务必须终止其进程树且不遗留孤儿。

## 14. 播放器与自动混音

用 Qt Multimedia 提供输入、音色、输出播放器：播放、暂停、进度和音量。

自动混音执行统一采样率、时长对齐、延迟校正、补零/裁切、响度匹配、混合、防削波和峰值限制，输出 WAV，并可导出 FLAC/MP3。用户只看到“人声更突出 / 均衡 / 伴奏更突出”。输出必须有限、非静音，峰值符合限制。

## 15. Windows 硬件、显存与自动降级

启动检测 Windows、CPU、RAM、GPU、驱动、VRAM、Compute Capability、CUDA、FP16、FFmpeg 和磁盘，并执行真实 CUDA smoke test。

显存档位：≤4GB 极低、4～6GB 低、6～10GB 标准、>10GB 高质量。档位应真实影响 MSST 模型/分段、batch、FP16、模型加载、Vevo2、DiffSinger、CPU offload 和缓存清理。

明确 CUDA OOM 时有限降级：结束失败 worker、释放上下文、清显存、缩短分段、降 batch、关闭增强、使用轻量 MSST、必要时 Vevo2 → DiffSinger，然后有限重试。不得无限重试或把一般错误误判为 OOM。

## 16. Modern、Legacy 与发行结构

提供：

- `OpenCoverStudio-NVIDIA-Modern`：Windows 10/11、RTX、建议 8GB+、CUDA 12 系；原词完整，Vevo2 Beta，条件 DiffSinger。
- `OpenCoverStudio-NVIDIA-Legacy`：GTX 10/16、老 RTX、4～8GB、CUDA 11.8；轻量 MSST、RVC、DDSP、原词完整，条件 GAME+DiffSinger，不强制 Vevo2。GTX 900 仅实验性。

没有相应实体旧显卡测试时，只能称为 Legacy 构建目标，不能声称兼容已验收。

采用免安装 ZIP/目录，优先 PyInstaller onedir 或经评估的 pyside6-deploy/Nuitka；不要制作几十 GB 单文件 EXE。发行目录结构清楚，GUI 为 windowed，独立 Worker 隐藏控制台但保留 UTF-8 协议、日志和取消能力。

拆分基础包与改词扩展：基础包目标包括 GUI、FFmpeg、MSST、RVC、DDSP、常用分离模型、初始音色、祥子双模型、头像、标准干声和混音；改词扩展包括 Vevo2、GAME、DiffSinger、vocoder、G2P 和对齐模型。受许可证限制的模型不能进入公开基础包，应提供合法安装路径或标明本机私有包边界。改词扩展缺失时 GUI 显示预计下载大小和安装入口。

## 17. 目录、代码质量与 Git

保持 GUI、Pipeline、Adapter、Model、Worker、Storage 分层；使用类型标注、Pydantic、`pathlib`、日志和安全进程参数。重型后端使用独立环境。所有用户路径、日志和协议支持中文。

Git 忽略至少包括下载缓存、后端、运行时、权重、私有包、工作区、虚拟环境、构建、发行目录以及 `*.pth/*.pt/*.ckpt/*.index/*.onnx`；只有合法的小型测试 fixture/试听源可明确例外。用户歌曲与授权不清权重不得提交。

开发工作应实际编辑、运行、修复、测试、构建并按逻辑提交；不只输出设计、伪代码或 TODO。任何已有用户改动必须保留。

## 18. 必需测试矩阵

自动化测试至少覆盖：

- GUI 构造、DPI、拖放、播放器、引擎切换；
- RVC/DDSP 导入、RVC 无 index、DDSP 配置、中文路径、重复哈希；
- 头像上传、真实 MIME/图片校验、EXIF、占位头像；
- 用户上传试听、模型自带试听、无试听自动生成；
- 后端未安装、自动生成失败、取消生成、删除/重生试听；
- RVC 与 DDSP 真实 preview、pitch 变化、preview 缓存；
- 输入标准干声不能冒充输出，其他模型 preview 不能复用；
- 下载续传/校验、安全解压、路径越界、覆盖保护；
- MSST、RVC、DDSP、Vevo2、GAME、DiffSinger、歌词对齐；
- 自动混音、缓存键、任务取消/恢复、历史、日志、托盘；
- CUDA OOM 有限降级、中文路径、Modern/Legacy 构建与泄漏审计。

单元测试只能证明其覆盖范围。后端“真实可用”还必须有目标模型真实运行输出、时长/采样率/声道/峰值/RMS/finite、输入输出哈希不同、日志和版本/哈希证据。

## 19. 最终验收清单

最终交付必须逐项给出证据：

- [ ] 从原空文件夹形成完整项目，Git 历史存在且工作区状态明确；
- [ ] PySide6 Qt Widgets 七页 GUI，双击源码和 EXE 都能打开；
- [ ] 无浏览器、无用户可见终端、拖放与中文路径正常；
- [ ] RVC/DDSP 二级选择正确，切换不保留错误模型；
- [ ] GUI 可导入 RVC 和 DDSP、上传头像/试听、编辑元数据；
- [ ] 所有内置/用户音色有合法头像或项目占位头像；
- [ ] 后端缺失时无虚假试听，后端存在时能后台真实生成；
- [ ] 标准干声来源和再分发权明确；
- [ ] 每份自动试听由目标模型生成并通过非复制/非静音验证；
- [ ] MSST + 普通 RVC 与 MSST + 祥子 RVC 真实成功；
- [ ] MSST + 普通 DDSP 与 MSST + 祥子 DDSP 真实成功；
- [ ] 自动混音与 WAV/FLAC/MP3 导出真实有效；
- [ ] 任务取消、恢复、缓存、历史、日志、后台和托盘可用；
- [ ] Vevo2 中文与日文真实短句通过后才显示可用；
- [ ] GAME + G2P + DiffSinger 固定样例真实 WAV；
- [ ] RVC 3～5 和 DDSP 3～5 初始音色达到来源/许可/头像/试听标准，或对尚未满足项明确标为未完成；
- [ ] `toyokawa_sakiko_rvc/ddsp` 固定 ID、来源、哈希、本机真实推理与发行边界正确；
- [ ] 下载器与 GUI 组件安装满足续传、进度、校验、安全与取消；
- [ ] Modern/Legacy 均构建，公开包通过用户歌曲和受限权重泄漏审计；
- [ ] 硬件探测和真实 CUDA smoke 有证据，未测试硬件不夸大；
- [ ] 所有资源均有来源、固定版本、大小、哈希、许可证和状态；
- [ ] 自动化测试、真实后端测试、中文文档、发行说明和 Git 提交完整；
- [ ] 没有 mock 或伪造输出冒充任何已完成功能。

## 20. 最终报告必须回答的内容

最终报告至少列明：GUI 页面；EXE/终端状态；后端及版本；MSST 模型；RVC/DDSP 初始音色；每个头像与试听来源；标准干声与许可证；祥子双模型来源和真实结果；模型/头像/试听的导入方式；RVC/DDSP 自动试听证据；MSST+RVC/DDSP 输出；Vevo2 中日文；GAME+DiffSinger；Modern/Legacy 范围；实测 GPU；公开及私有包体积；自动化和真实测试结果；Git 提交；许可证/硬件/维护风险；尚未完成项和下一优先级。

报告中的“完成”必须和本文件验收项一一对应。若有网络、许可证、显存、平台或实体硬件限制，应给出证据、当前安全边界及继续路径；真实可运行的阶段版本优于未经验证的功能堆砌，但阶段版本不得被称为完整最终交付。
