# C 流程通用化与软件实装验证

2026-09-08，工作流 `clear_c_v1` 已接入改词任务默认入口。没有切换生成器，旧 C/C1 试听文件保留。

## 验证结果

| 检查 | 实际结果 |
| --- | --- |
| 自动化测试 | 134 项通过，覆盖移位区间、不同长度间隙、连续改词、伴奏参考缺失、句间恒定增益、静音保留及 GUI 入口。 |
| 离屏 GUI | 改词页面正常展示原曲、新旧歌词、原生歌声音色及自动处理说明；截图保存在本次工作目录。 |
| 打包入口 | 项目根目录 GUI 与独立 worker 已构建；长、短任务均实际调用打包后的 worker。 |
| 长段 GPU 任务 | 29.75 秒、8 个改词区间、7 个合并演唱短句，真实 GAME + DiffSinger 推理和最终导出完成。GAME 日志记录 RTX 5070 Ti Laptop GPU；DiffSinger 逐句记录 `device: cuda`。 |
| 普通文本短任务 | 12 秒输入，原词、新词均不带时间戳，自动定位并只生成修改的 1 句，另一句保留原声。真实 GPU 推理与导出完成。 |
| 源码与打包一致性 | 长段最终混音逐采样一致。 |
| C 歌声一致性 | 均衡后主唱与认可的 C 主唱最大采样差 `1.49e-8`，属于浮点舍入量。旧基线文件 SHA-256 保持一致。 |
| 未改词音频 | 长、短两个任务均逐采样保留标准化原曲的未改词部分。 |
| 吐字复核 | 均衡后主唱识别为全部 63 个目标字；相同句界的 C/新混音对照均识别全部 63 字。 |
| 音高复核 | 各句相对内部词谱的音高误差中位数 10–20 音分，约 97.1%–100% 有效帧落在一个半音内。该指标衡量执行词谱的准确度，不替代原曲旋律与听感验收。 |

整段混音直接做能量自动分句时，旧 C 与新混音被切到不同位置，新混音产生了“奔向”误识别和句尾“径”遗漏等结果。记录未删除。随后采用同一组歌词间隙切分进行 A/B 检查，两份混音均识别 63/63；这用于排除分句差异，不把 ASR 当作听感验收。

## 两处衔接

试听剪辑取完整音频 2–16 秒，保持旧 C 试听的同一增益。下表为边界前后各 25 ms 的 RMS 差；原曲本身有音乐起伏，因此修复目标是去掉额外突变。

| 试听位置 | 旧 C | 新工作流 | 原曲音乐起伏 |
| --- | ---: | ---: | ---: |
| 6.07 秒 | +9.409 dB | +2.432 dB | +2.424 dB |
| 6.45 秒 | −7.443 dB | −0.557 dB | −0.548 dB |
| 12.54 秒 | +8.257 dB | +1.342 dB | +1.296 dB |
| 12.73 秒 | −7.341 dB | −0.244 dB | −0.236 dB |

长段输出峰值 0.713，没有额外峰值衰减。所有边界取得可靠伴奏增益参考；短间隙参考不足时使用同曲其他音乐间隙，并保留估计来源。处理规则遍历全部区间，不包含样例的歌曲名或秒数。

## 本地证据

- 工作目录：`workspace/workflow_20260908`。
- 长段打包任务：`long_frozen/request.json`、`worker.log`、`validation.json`。
- 普通文本任务：`short_plain_frozen/request.json`、`worker.log`、`validation.json`。
- 采样与边界验证：`audio_verification.json`。
- 相同句界识别：`fixed_windows_asr.json`；原始自动分句结果位于长段任务目录。
- 音高复核：`long_source/melody_audit.json`，对应与打包输出一致的源码生成结果。
- GUI 截图：`lyric_gui.png`。
- 新试听：`long_frozen/C_workflow_14s.wav`。
- 安装清单及 EXE 哈希：`workspace/local_app_build/installed.json`。

当前验证覆盖已有中文回归样例和不同区间结构的自动化测试，不代表所有歌曲、语种和音色的听感都已验收。新混音仍保留用户试听确认环节。
